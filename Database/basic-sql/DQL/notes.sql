-- 🔹 What is DQL?
-- Definition: DQL(data query language) is used to query data stored in tables.

-- Key Point: It is read-only — it does not insert, update, or delete data.

-- Main Command: SELECT (with clauses like WHERE, GROUP BY, HAVING, ORDER BY, LIMIT).

create database DQL;
use dql;